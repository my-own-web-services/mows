manifestVersion: "0.1"
metadata:
    name: pektin
    description: "Pektin DNS"
    version: "0.1"
spec:
    raw:
        helmRepos:
        -   chartName: pektin
            releaseName: pektin
            repository: 
                local:
            valuesFile: values.yaml

