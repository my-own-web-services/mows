manifestVersion: "0.1"
metadata:
    name: vault
    description: "vault"
    version: "0.1"
spec:
    raw:
        helmRepos:
        -   chartName: vrc
            releaseName: vrc
            repository:
                local:
            valuesFile: files/vrc-values.yaml
