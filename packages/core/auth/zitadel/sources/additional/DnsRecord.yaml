apiVersion: pektin.k8s.mows.cloud/v1
kind: PektinDns
metadata:
    name: zitadel-dns
spec:
    plain:
        - name: "zitadel.{§ .config.domain §}."
          meta: ""
          ttl: 60
          rr_set:
              A:
                  - value: "{§ .config.legacy_ip §}"
