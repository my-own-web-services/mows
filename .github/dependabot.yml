version: 2
updates:
  # GitHub Actions - monthly updates for pinned SHA hashes
  # Security updates are always created regardless of schedule
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    commit-message:
      prefix: "ci"
    labels:
      - "dependencies"
      - "github-actions"

  # Rust dependencies for mows-cli - monthly updates
  # Security updates are always created regardless of schedule
  - package-ecosystem: "cargo"
    directory: "/utils/mows-cli"
    schedule:
      interval: "monthly"
    commit-message:
      prefix: "deps(mows-cli)"
    labels:
      - "dependencies"
      - "rust"
