# tab lines

# brighter colors

# suggestions

# file linting based on schema

# warning for empty variables

# template before and after render line comparison side by side

# true and := coloring

no tests can ever touch the real config file, there is a env variable to set an alternative path for that

add comments to each generated/template result that tell that this file should not be edited as it is generated

tests running in docker cannot leave dangling containers around