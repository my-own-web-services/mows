//! Access-policy storage + CRUD.
//!
//! The full implementation lands in Phase 1 — see ROADMAP.md. This
//! module currently exposes the canonical `AccessPolicy` struct shape
//! so downstream services can type-check their wiring; the SQL paths
//! arrive when the move from filez completes.

// TODO Phase 1: bring over filez's `AccessPolicy` struct and its
// CRUD helpers from `apis/cloud/filez/server/src/models/access_policies/`.
// Until then this is a marker module.
