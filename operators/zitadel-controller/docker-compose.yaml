services:
    zitadel-controller:
        build:
            context: "."
            dockerfile: Dockerfile
            tags: 
                - localhost:5000/zitadel-controller
            additional_contexts:
                mows-common: ../../utils/mows-common
                zitadel-rust: ../../utils/zitadel-rust
                lock: ../../
            args:
                - PROFILE=dev # dev or release


