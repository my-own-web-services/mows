apiVersion: vault.k8s.mows.cloud/v1
kind: VaultResource
metadata:
    name: pektin-transit # can be anything
    namespace: pektin
spec:
    SecretEngine:
        engineType: transit
        engineName: dns-signing
# should create a secret engine of type transit at the path mows-core-secrets-vrc/pektin/dns-signing
