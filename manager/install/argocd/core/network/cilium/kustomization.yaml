apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
    - announce.yaml
    - ip-pool.yaml
    - namespace.yaml

helmCharts:
    - name: cilium
      repo: https://helm.cilium.io
      version: 1.15.0
      releaseName: mows-network
      includeCRDs: true
      namespace: mows-network
      valuesFile: values.yaml
