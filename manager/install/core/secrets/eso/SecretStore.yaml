apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
    name: default
    namespace: mows-secrets-eso
spec:
    provider:
        vault:
            server: http://mows-secrets-vault.mows-secrets-vault:8200
            version: v2
